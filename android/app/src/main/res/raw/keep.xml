<?xml version="1.0" encoding="utf-8"?>
<!-- Resources named only from Dart are invisible to the release build's
     resource shrinker, which removes them. The notification's picture and
     its sound are both looked up by name at runtime. -->
<resources xmlns:tools="http://schemas.android.com/tools"
    tools:keep="@drawable/ic_notify_mascot,@raw/rewire_reward3,@drawable/ic_stat_rewiremind" />
